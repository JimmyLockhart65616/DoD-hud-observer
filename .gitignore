node_modules
config.json
backend/src/db/db.json
game/addons/amxmodx/scripting/Events.amxx
game/addons/amxmodx/scripting/Events.sma.bak
game/addons/amxmodx/scripting/Events.smaa
current.code-workspace

# DoD server — AMXX binaries and compiled plugin (too large / platform-specific)
dod-server/mods/addons/amxmodx/dlls/
dod-server/mods/addons/amxmodx/modules/
dod-server/mods/addons/amxmodx/plugins/*.amxx
dod-server/mods/addons/metamod/dlls/
dod-server/mods/addons/metamod/*.so
dod-server/mods/addons/metamod/*.dll
dod-server/mods/addons/metamod/example_plugin/
dod-server/mods/addons/metamod/sdk/

# Match recordings (generated at runtime)
matches/

# Playwright E2E test artifacts
e2e/snapshots/
test-results/
playwright-report/

# Deploy — the real systemd unit contains the HUD_AUTH_KEY secret.
# Checked-in counterpart: deploy/hud-observer.service.example
deploy/hud-observer.service
deploy/hud_observer.cfg

# Production backend config — contains ingest auth_key + HLTV rcon passwords.
# Checked-in counterpart: config/online/config.yaml.example
config/online/config.yaml

# Compiled plugin — build output, not source
/KTPHudObserver.amxx

# Local reference files (event captures, external .sma refs) — not for commit
/.local-refs/

# Build artifacts from local HLDS/AMXX Docker builds
/.build-output/

# Claude Code per-machine tool-permission settings — not for commit
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/hooks/

# Weapon-sprite SOURCE BMPs: 18 MB extracted, but compress to a 38 KB zip. Commit
# assets/weapons-src.zip (provenance); the extracted working dir is local-only.
# Regenerate the committed PNGs with `npm run assets:weapons`.
/assets/weapons-src/

# HLTV↔overlay sync soak CSVs pulled off the data server (131 MB, 36 files).
# The evidence trail behind SYNC-MEASUREMENT.md "RESOLVED (2026-07-03)"; kept
# locally after the prod monitor was torn down 2026-08-02. Too large to commit.
/e2e/repro/soak-logs/

# Local TLS certs for the docker nginx :443 mirror — machine-specific mkcert
# output (or a self-signed fallback), never committed. Generate per
# deploy/README.md → Friendly URL.
/data-server/certs/